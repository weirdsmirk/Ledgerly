# Dependencies
node_modules/

# Build output
dist/
*.tsbuildinfo

# Local database (contains your real financial data — never commit)
backend/app.db
backend/app.db-*
backend/*.db
backend/*.db-*

# Environment files (local overrides, e.g. PORT / DB_PATH / VITE_API_URL)
.env
.env.local
.env.*.local
!.env.example

# Logs & OS noise
*.log
npm-debug.log*
.DS_Store

# Editor / tooling
.vscode/
.idea/

# Local tooling (skills, design docs — not for repo)
.opencode/
.impeccable/