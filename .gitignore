# Dependencies
node_modules/

# Build output
dist/
*.tsbuildinfo

# Local SQLite database (single file in the data folder at the project root)
data/*.sqlite
data/*.sqlite-wal
data/*.sqlite-shm

# Environment files (local overrides, e.g. PORT / DB_PATH / VITE_API_URL)
.env
.env.local
.env.*.local
!.env.example

# Logs & OS noise
*.log
npm-debug.log*
.DS_Store

# Editor / tooling
.vscode/
.idea/

# Local tooling (skills, design docs — not for repo)
.opencode/
.impeccable/
