# Frontend environment overrides (copy to frontend/.env and adjust as needed)

# Where the Vite dev server should call the API.
# Dev default: http://localhost:8734/api
# Leave unset (or set to /api) in production — the backend serves the built
# frontend on the same origin.
VITE_API_URL=http://localhost:8734/api